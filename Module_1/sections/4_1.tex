Typically, the search is done by a \textbf{Backtracking Tree Search (BTS)} algorithm that enumerates all possible variable-value combinations to find a solution.
The search tree is built gradually: everytime we need to assign a value to a decision variable a new node representing that variable is created and all 
the branches that come out of it define the allowed decisions.